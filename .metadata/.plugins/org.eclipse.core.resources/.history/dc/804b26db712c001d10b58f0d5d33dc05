public class InheritanceB {
    static float length,breadth,area;
    public float rectangleArea(float l, float b){
        length=l;
        breadth=b;
        area=length*breadth;
        return(area);
    }
}
